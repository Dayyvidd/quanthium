import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const RequestScreen = () => {
    return <Text style={{fontSize: 48}}>RequestScreen</Text>;
};

const styles = StyleSheet.create({});

export default RequestScreen;