import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const FriendScreen = () => {
    return <Text style={{fontSize: 48}}>FriendScreen</Text>;
};

const styles = StyleSheet.create({});

export default FriendScreen;